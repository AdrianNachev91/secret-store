<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>photos.sluice</groupId>
  <artifactId>secret-store-example</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>secret-store-example</name>
  <description>A command-line CRUD over one credential, resolving secret-store as a released
    artifact rather than as a module of its build.</description>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- The version that opened the current major, so building this example resolves the artifact
         from Central rather than a local build. It moves on a major and not otherwise, the API
         being closed within one. -->
    <secret-store.version>1.0.0</secret-store.version>
  </properties>

  <dependencies>
    <!-- Released coordinates and not a reactor module. What a consumer depends on is the
         published artifact, so that is what this builds against. -->
    <dependency>
      <groupId>photos.sluice</groupId>
      <artifactId>secret-store</artifactId>
      <version>${secret-store.version}</version>
    </dependency>
  </dependencies>

  <build>
    <!-- Without this the jar carries the version, and every command in the README would have to
         be reworded on each release. -->
    <finalName>secret-store-example</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.16.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>photos.sluice.secrets.example.Main</mainClass>
              <!-- java -jar ignores any classpath given on the command line, so the library has
                   to be named in the manifest instead. The copy step below puts it where this
                   prefix points. -->
              <addClasspath>true</addClasspath>
              <classpathPrefix>lib/</classpathPrefix>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.11.0</version>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
